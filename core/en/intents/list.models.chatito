import ../common.chatito

%[list.models]('training': '100')
    ~[what-which] ~[make] ~[posseses] ~[spec]
    ~[what-which] ~[make?] ~[model-type] ~[posseses] ~[spec]
    ~[declaritive] ~[modifier-spec] what does ~[make] have
    ~[exist] ~[model-type] ~[posseses]
    ~[exist] ~[make] ~[posseses] ~[spec]